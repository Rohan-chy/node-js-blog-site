<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog Post</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            text-align: center;
        }

        h1 {
            font-size: 36px;
            margin: 20px 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            padding: 20px;
        }

        .author-info {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .author-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-right: 10px;
            background-color: #333;
        }

        .author-name {
            font-weight: bold;
        }

        .post-content {
            line-height: 1.6;
        }

        .post-footer {
            margin-top: 20px;
            font-style: italic;
        }
    </style>
</head>
<body>
    <header>
        <h1>CMS</h1>
    </header>

    <div class="container">
        <div class="author-info">
     
            <div class="author-name"><%- getId[0].title %></div>
        </div>
        <div class="author-name"><%- getId[0].subTitle %></div>

        <div class="post-content">
            <p><% getId[0].description %></p>

        </div>
        <div class="post-footer">
            Author: <%- getId[0].user.username %>
        </div>
        <img src="<%- getId[0].image %>" alt="image" height="150px" weight="150px">
        <div class="post-footer">
            Published on <%- getId[0].createdAt.toISOString().slice(0,10) %>
        </div>
        
       <% if(presentUser){ %>
            <button><a href="/delete/<%- getId[0].id %>">DELETE</a></button>
            <button><a href="/edit/<%- getId[0].id %>">EDIT</a></button>
     <% } %>
    </div>
</body>
</html>